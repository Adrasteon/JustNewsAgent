# JustNews V4 Runtime-Only Requirements
# Minimal production dependencies for Preview workspace deployment
#
# Usage:
#   python3 -m venv /opt/justnews/venv
#   /opt/justnews/venv/bin/pip install -r requirements-runtime.txt
#
# Version constraints aligned with environment.yml for consistency

# Core FastAPI stack for agent APIs
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# HTTP client for MCP inter-agent communication
requests>=2.31.0

# Observability and monitoring
prometheus-client>=0.19.0

# Database client (runtime PostgreSQL connections)
psycopg2-binary>=2.9.9
