# Minimal runtime-only dependencies for Preview CI validation
# This file contains only the Python packages needed to start and run
# JustNews agents, without development, testing, or full ML dependencies.
# Used by CI to validate deployment dependency management.

# Core web framework dependencies (essential for all agents)
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
requests>=2.31.0

# Database connectivity (required by memory agent and others)
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0

# Basic utilities used across agents
python-dotenv>=1.0.0
