# Example global.env for JustNews
# Copy or template this to /etc/justnews/global.env and edit values as needed.

# Path to the Python interpreter to run services. Fill with an absolute path to the conda env python
# Example: PYTHON_BIN=/home/adra/miniconda3/envs/justnews-v2-py312/bin/python
# If unset, the reset script will attempt auto-detection or leave a commented placeholder.
#PYTHON_BIN=/path/to/python

# Database configuration
postgres_host=localhost
postgres_db=justnews
postgres_user=justnews_user
postgres_password=REPLACE_ME

# Connection pool sizing
db_pool_min_connections=2
db_pool_max_connections=10

# SAFE_MODE can be true/false and will be set by the reset script when requested
#SAFE_MODE=false

# Model store configuration
# Path to a location on the host where large model artifacts and caches are stored.
# On developer machines this may be under /media/<user>/Data/justnews/model_store or
# under /var/lib/justnews/model_store on dedicated servers. Services will use
# ARCHIVE_KG_STORAGE (below) to place knowledge-graph state. The reset script will
# attempt to detect a suitable MODEL_STORE_ROOT if one is not provided.
# Example: MODEL_STORE_ROOT=/media/adra/Data/justnews/model_store
#MODEL_STORE_ROOT=/path/to/model_store

# Optional explicit override for knowledge-graph storage. When set this takes
# precedence over MODEL_STORE_ROOT. If unset, services will use
# ${MODEL_STORE_ROOT}/kg_storage by default.
#ARCHIVE_KG_STORAGE=/path/to/kg_storage
