# JustNews V4 - Base Requirements
#
# This file is a lightweight stub that redirects to the main dependency source.
# For development, use: conda env create -f environment.yml
# For minimal runtime, use: pip install -r release_beta_minimal_preview/requirements-runtime.txt
#
# This file exists for CI compatibility and contains only the most essential packages.

# Core web framework
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
requests>=2.31.0

# Core data science
numpy>=1.24.0
pandas>=2.0.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.21.1
